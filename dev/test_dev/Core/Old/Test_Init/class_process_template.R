#Timeline_R6.R
xxx = R6Class(
  "xxx",
  inherit = Process,
  private = list(
    .config = list(name = 'xxx',
                   steps = c(xxx),
                   mandatory = c(xxx)
    )
  ),
  
  public = list(
    
    Add_RenderUIs_Definitions = function(input, output){
     
      
    }
    
  )
)